{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability por defecto para la ventana principal",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
